# 📌 가상환경 · venv
# 가상환경은 프로젝트별로 독립된 파이썬 환경을 제공하여, 
# 패키지 충돌을 방지하고 프로젝트 관리를 용이하게 함

# 왜 필요한가?
# - 프로젝트 A: `requests 2.28`
# - 프로젝트 B: `requests 2.31`
# 👉 전역 설치면 충돌
# 👉 가상환경이면 **완벽 분리**

# 📌 가상환경 생성
# 1. 터미널 열기
# 2. 프로젝트 디렉토리로 이동
# cd path\to\your\project
# 3. 가상환경 생성 명령어 실행
#    - Windows: `python -m venv venv`
#    - macOS/Linux: `python3 -m venv venv`
# 4. `venv` 폴더 생성 확인

# 📌 가상환경 활성화
# - Windows: `venv\Scripts\activate`
# - macOS/Linux: `source venv/bin/activate`
# 활성화되면 프롬프트에 `(venv)` 표시
#  (venv) C:\path\to\your\project>

# 📌 가상환경 비활성화
# - `deactivate` 명령어 입력
# (venv) C:\path\to\your\project> deactivate
# 프롬프트에서 `(venv)` 사라짐

# 📌 가상환경 삭제
# 1. 가상환경 비활성화
# 2. `venv` 폴더 삭제
# rm -rf venv  # macOS/Linux
# rmdir /s venv  # Windows
# 가상환경 삭제는 프로젝트에 영향을 주지 않음

# 📌 가상환경에서 pip 확인
# - `pip list` 명령어로 설치된 패키지 확인
# - `pip install package_name` 명령어로 패키지 설치
# - `pip uninstall package_name` 명령어로 패키지 제거
# - `pip freeze > requirements.txt` 명령어로 현재 패키지 목록 저장
# - `pip install -r requirements.txt` 명령어로 패키지 목록 설치
# - `pip --version` 명령어로 pip 버전 확인

# 📌 VS Code에 venv 연결 ✨✨✨ =========================
# 1. VS Code에서 프로젝트 폴더 열기
# 2. Ctrl + Shift + P 눌러 명령 팔레트 열기
# 3. "Python: Select Interpreter" 입력 후 선택
# 4. 목록에서 `venv` 가상환경 선택
# 5. 터미널 열기 (Ctrl + `) → 가상환경 자동 활성화 확인
# 확인 방법
# VS Code 우측 하단 -- Python 3.x (venv)

# 📌 실무용 프로젝트 구조 예시 =========================
# project/
#  ├─ venv/
#  ├─ src/
#  │   ├─ main.py
#  │   ├─ utils.py
#  │   └─ service/
#  │       └─ logic.py
#  ├─ data/
#  │   └─ input.txt
#  ├─ tests/
#  │   └─ test_main.py
#  ├─ requirements.txt
#  └─ README.md
