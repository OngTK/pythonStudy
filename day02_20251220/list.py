# 📌List
# 리스트는 여러 개의 값을 하나의 변수에 저장할 수 있는 자료형
# 리스트는 대괄호 []로 감싸며, 각 값은 쉼표(,)로 구분
# 리스트는 다양한 메서드(method)를 제공하여 값을 추가, 삭제, 수정, 검색
# 리스트는 다양한 자료형의 값을 포함할 수 있음 (숫자, 문자열, 불리언 등)
# 리스트는 순서가 있는 자료형으로, 인덱스를 사용하여 각 값에 접근 가능
# 리스트는 변경 가능한(mutable) 자료형
# ======================================================================

# 리스트 생성
fruits = ['apple', 'banana', 'cherry']

# 📌리스트 인덱스(Index)=================================================
# 리스트의 각 값은 인덱스를 통해 접근 가능
# 인덱스는 0부터 시작
# 음수 인덱스를 사용하여 뒤에서부터 접근 가능
print("리스트 인덱스(Index)")
print(fruits[0])    # 'apple'
print(fruits[1])    # 'banana'
print(fruits[2])    # 'cherry'

print("\n음수 인덱스---")
print(fruits[-1])   # 'cherry'
print(fruits[-2])   # 'banana'
print(fruits[-3])   # 'apple'


# 📌리스트 값 추가 ================================================
numbers = [1, 2, 3, 4, 5]
print("원본 리스트:", numbers)

print("\n리스트 값 추가")
# append() : 리스트 끝에 값을 추가
numbers.append(6)  # 리스트 끝에 값 추가
print("append(6):", numbers)

# insert() : 특정 인덱스에 값을 추가
# 인덱스 2에 값 100 추가
# 기존 인덱스 2 이후의 값들은 한 칸씩 뒤로 밀림
numbers.insert(2, 100)  # 인덱스 2에 값 100 추가
print("insert(2, 100):", numbers)

# 📌리스트 값 수정 ================================================
print("\n리스트 값 수정")
numbers[2] = 10
print("수정된 리스트:", numbers)

# 📌리스트 값 삭제 ===============================================
# remove() 메서드는 특정 ✨값✨을 삭제
print("\n리스트 값 삭제")
numbers.remove(10)  # 값 10 삭제
print("remove(10):", numbers)

# pop() 메서드는 특정 ✨인덱스✨를 삭제하고 반환
popped_value = numbers.pop(2)  # 인덱스 2의 값 삭제
print("pop(2):", numbers)

# del 키워드는 특정 ✨인덱스✨를 삭제
del numbers[0]  # 인덱스 0의 값 삭제
print("del numbers[0]:", numbers)
# clear() 메서드는 리스트의 모든 값을 삭제
numbers.clear()  # 모든 값 삭제
print("clear():", numbers)

# 📌리스트 값 검색 ================================================
print("\n리스트 값 검색")
fruits = ['apple', 'banana', 'cherry', 'date', 'elderberry']
# index() 메서드는 특정 값의 인덱스를 반환
index_banana = fruits.index('banana')
print("index('banana'):", index_banana)     # 출력 : 1

# count() 메서드는 특정 값의 개수를 반환
count_cherry = fruits.count('cherry')
print("count('cherry'):", count_cherry)     # 출력 : 1

# in 키워드는 특정 값이 리스트에 존재하는지 확인
has_date = 'date' in fruits
print("'date' in fruits:", has_date)        # 출력 : True
has_fig = 'fig' in fruits
print("'fig' in fruits:", has_fig)          # 출력 : False

# 📌리스트 길이 len() ================================================
print("\n리스트 길이 len()")
length = len(fruits)
print("리스트 길이:", length)  # 출력 : 5

# 📌리스트 슬라이싱(Slicing) ===============================================
# 리스트의 일부분을 추출하는 방법
print("\n리스트 슬라이싱(Slicing)")
sliced_fruits = fruits[1:4]  # 인덱스 1 부터 3까지 추출
print("fruits[1:4]:", sliced_fruits)  # 출력 : ['banana', 'cherry', 'date']

# 시작 인덱스를 생략하면 0부터 시작
sliced_fruits_start = fruits[:3]  # 인덱스 0 부터 2까지 추출
print("fruits[:3]:", sliced_fruits_start)  # 출력 : ['apple', 'banana', 'cherry']

# 끝 인덱스를 생략하면 리스트의 끝까지 추출
sliced_fruits_end = fruits[2:]  # 인덱스 2 부터 끝까지 추출
print("fruits[2:]:", sliced_fruits_end)  # 출력 : ['cherry', 'date', 'elderberry']  
# 음수 인덱스를 사용하여 뒤에서부터 추출
sliced_fruits_negative = fruits[-3:]  # 뒤에서 3개 추출 
print("fruits[-3:]:", sliced_fruits_negative)  # 출력 : ['cherry', 'date', 'elderberry']

# 📌리스트 반복 ===============================================
# for문을 사용하여 리스트의 각 값을 반복
print("\n리스트 반복")
names = ["Alice", "Bob", "Charlie"]

# for문으로 리스트 순회
for name in names:
    print(name)   
# Java의 향상된 for문과 유사한 기능

# index와 함께 반복
print("\nindex와 함께 반복")
for index in range(len(names)):
    print(f"Index {index}: {names[index]}") 
 
# 📌 리스트 정렬 ==============================================
print("\n리스트 정렬")
numbers = [5, 2, 9, 1, 5, 6]
print("원본 리스트:", numbers)  
# sort() 메서드는 리스트를 오름차순으로 정렬
numbers.sort()
print("sort():", numbers)
# reverse() 메서드는 리스트의 순서를 반대로 뒤집음
numbers.reverse()
print("reverse():", numbers)

# 📌 리스트 복사 ==============================================
print("\n리스트 복사")
original_list = [1, 2, 3, 4, 5]
# copy() 메서드는 리스트의 얕은 복사본을 생성
copied_list = original_list.copy()
print("원본 리스트:", original_list)
print("복사된 리스트:", copied_list)

# 📌 리스트 확장 =============================================
# extend() 메서드는 리스트에 다른 리스트를 추가
print("\n리스트 확장")
list1 = [1, 2, 3]
list2 = [4, 5, 6]

print ("원본 list", list1, list2)

list1.extend(list2)
print("extend(list2):", list1)  # 출력 : [1, 2, 3, 4, 5, 6]

# 📌 리스트 합치기 ============================================
# + 연산자는 두 리스트를 결합하여 새로운 리스트를 생성
print("\n리스트 합치기")
list3 = [7, 8, 9]
combined_list = list1 + list3
print("list1 + list3:", combined_list)  # 출력 : [1, 2, 3, 4, 5, 6, 7, 8, 9]

# 📌 리스트 반복하기 ===========================================
# * 연산자는 리스트를 반복하여 새로운 리스트를 생성
print("\n리스트 반복하기")
repeated_list = list3 * 3
print("list3 * 3:", repeated_list)  # 출력 : [7, 8, 9, 7, 8, 9, 7, 8, 9]

# 📌 리스트 내포(List Comprehension) =============================
# 리스트 내포는 기존 리스트를 기반으로 새로운 리스트를 생성하는 간결한 방법
print("\n리스트 내포(List Comprehension)")
squared_numbers = [x**2 for x in range(1, 6)]
print("squared_numbers:", squared_numbers)  # 출력 : [1, 4, 9, 16, 25]

# 짝수만 포함하는 리스트 생성
even_numbers = [x for x in range(1, 11) if x % 2 == 0]
print("even_numbers:", even_numbers)  # 출력 : [2, 4, 6, 8, 10]

# 📘 예제 1
# 빈 리스트를 만들고 숫자 5개를 입력받아 리스트에 저장한 후 출력하세요.
numbers = []
for i in range(5):
    # num = int(input("숫자를 입력하세요: ")) # 주석 처리하여 입력 부분 생략
    numbers.append(num)
print("입력된 숫자 리스트:", numbers)

# 📘 실습 2
# 리스트 `[1, 2, 3, 4, 5]`에서 짝수만 출력하세요.
numbers = [1,2,3,4,5]
for num in numbers:
    if num % 2 == 0 :
        print(num)  

# ✅ 리스트 정리
# 리스트는 여러 값을 하나의 변수에 저장하는 자료형
# 인덱스를 사용하여 값에 접근하고, 다양한 메서드를 통해 값을 추가, 삭제, 수정, 검색 가능
# 리스트는 순서가 있으며, 변경 가능한(mutable) 자료형
# 리스트 내포(List Comprehension)를 사용하여 간결하게 새로운 리스트를 생성할 수 있음
# 리스트는 파이썬에서 매우 자주 사용되는 자료형으로, 데이터 구조를 다룰 때 필수적인 개념

# ✅ 함수 정리
# append(): 리스트 끝에 값을 추가
# insert(): 특정 인덱스에 값을 추가
# remove(): 특정 값을 삭제
# pop(): 특정 인덱스의 값을 삭제하고 반환
# clear(): 리스트의 모든 값을 삭제
# index(): 특정 값의 인덱스를 반환
# count(): 특정 값의 개수를 반환
# sort(): 리스트를 오름차순으로 정렬
# reverse(): 리스트의 순서를 반대로 뒤집음
# copy(): 리스트의 얕은 복사본을 생성
# extend(): 리스트에 다른 리스트를 추가
# len(): 리스트의 길이를 반환
# in 키워드: 특정 값이 리스트에 존재하는지 확인
