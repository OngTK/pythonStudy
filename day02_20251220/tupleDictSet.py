# 📌 tuple · dict · set

# 📌 1. 튜플 · Tuple =============================================
# - 여러 개의 값을 묶어서 하나의 값으로 표현하는 자료형
# - 리스트와 유사하지만, 한 번 생성된 후에는 값을 변경할 수 없음(immutable)
# - 소괄호 () 로 표현 
# - 읽기 전용 데이터에 적합 = 수정이 불가한 방지용 데이터에 적합

point = (2, 3)
print("튜플 point:", point)
print("튜플 point[0]:", point[0])
print("튜플 point[1]:", point[1])

# 🟢 ex
user = ('admin', '1234')
print("아이디:", user[0])
print("비밀번호:", user[1])

# 📌 2. 딕셔너리 · Dictionary ============================================
# - 키(key)와 값(value)의 쌍으로 구성된 자료형
# - 중괄호 {} 로 표현
# - 키를 사용하여 값에 접근
# - 변경 가능한(mutable) 자료형
# - JSON(JavaScript Object Notation)과 유사한 구조

person = {
    'name': 'Alice',
    'age': 30,
    'city': 'New York'
}
print("\n딕셔너리 person:", person)
print("person['name']:", person['name'])
print("person['age']:", person['age'])
print("person['city']:", person['city'])

# 📌 값 추가
print("\n딕셔너리 값 추가")
person['job'] = 'Engineer'  # 새로운 키-값 쌍 추가
print("추가된 딕셔너리 person:", person)

# 📌 값 수정
print("\n딕셔너리 값 수정")
person['age'] = 31  # 기존 키의 값 수정
print("수정된 딕셔너리 person:", person)

# 📌 값 삭제
print("\n딕셔너리 값 삭제")
del person['city']  # 키-값 쌍 삭제
print("삭제된 딕셔너리 person:", person)
# pop() 메서드를 사용하여 키-값 쌍 삭제
job = person.pop('job')  # 'job' 키-값 쌍 삭제
print("pop된 값:", job)
print("삭제된 딕셔너리 person:", person)

# 📌 반복문을 통한 확인
print("\n딕셔너리 반복문")
for key in person:
    print(f"{key}: {person[key]}")  

# 📌 자주쓰는 메서드
print("\n딕셔너리 메서드")
print("keys():", person.keys())        # 키 목록 반환
print("values():", person.values())    # 값 목록 반환
print("items():", person.items())      # 키-값 쌍 목록 반환

# 📌 3. 집합 · Set ================================================
# - 중복되지 않는 유일한 값들의 모음
# - 중괄호 {} 로 표현
# - 순서가 없으며, 인덱스를 사용하여 접근 불가
# - 변경 가능한(mutable) 자료형

colors = {'red', 'green', 'blue'}
print("\n집합 colors:", colors)

# 📌 값 추가 .add()
print("\n집합 값 추가")
colors.add('yellow')  # 값 추가
print("추가된 집합 colors:", colors)

# 📌 값 삭제 .remove()
print("\n집합 값 삭제")
colors.remove('green')  # 값 삭제
print("삭제된 집합 colors:", colors)

# 📌 집합 연산
print("\n집합 연산")
setA = {1, 2, 3, 4}
setB = {3, 4, 5, 6}
print("집합 A:", setA)
print("집합 B:", setB)

# 합집합
print( setA | setB )
print("합집합 A ∪ B:", setA.union(setB))
# 교집합
print( setA & setB )
print("교집합 A ∩ B:", setA.intersection(setB))
# 차집합
print( setA - setB )
print("차집합 A - B:", setA.difference(setB))

# 대칭차집합
print("대칭차집합 A △ B:", setA.symmetric_difference(setB))
# 부분집합 확인
print("A가 B의 부분집합인가?", setA.issubset(setB))
print("B가 A의 부분집합인가?", setB.issubset(setA))
# 진부분집합 확인   
print("A가 B의 진부분집합인가?", setA < setB)
print("B가 A의 진부분집합인가?", setB < setA)

# ✅ 자료구조 비교
# 자료형   | 변경 | 중복 | 순서 | 사용 목적
# --------|------|------|------|----------------------
# 리스트   | O    | O    | O    | 순서가 있는 데이터 저장
# 튜플     | X    | O    | O    | 읽기 전용 데이터 저장
# 딕셔너리 | O    | key X| X    | 키-값 쌍으로 데이터 저장
# 집합    | O    | X    | X    | 유일한 값들의 모음 저장

# ✅ 정리
# - 튜플은 변경 불가능한 순서가 있는 자료형
# - 딕셔너리는 키-값 쌍으로 구성된 변경 가능한 자료형
# - 집합은 중복되지 않는 유일한 값들의 모음
# - 각 자료형은 특정 상황에 맞게 사용되어야 함

# ✅ Tuple 메소드
# 튜플은 변경 불가능한 자료형이므로 메서드가 제한적임
# count() - 특정 값의 개수를 반환
# index() - 특정 값의 인덱스를 반환

# ✅ 딕셔너리 메소드
# keys() - 모든 키를 반환
# values() - 모든 값을 반환
# items() - 모든 키-값 쌍을 반환
# get() - 키에 해당하는 값을 반환(키가 없으면 None 또는 기본값 반환)
# pop() - 특정 키-값 쌍을 삭제하고 값을 반환

# ✅ set 메소드
# add() - 값 추가
# remove() - 특정 값을 삭제(값이 없으면 오류 발생)
# discard() - 특정 값을 삭제(값이 없어도 오류 발생하지 않음)

# 📘 예제 1
# 튜플로 좌표(x,y)로 만들고 출력하세요
coordinate = (10, 20)
print("\n예제 1 - 좌표 출력")
print("좌표:", coordinate)

# 📘 예제 2
# 딕셔너리로 학생 정보(이름, 나이, 점수)를 저장하고 점수만 출력하세요.
student = {
    'name': 'Bob',
    'age': 22,
    'score': 95
}
print("\n예제 2 - 학생 점수 출력")
print("학생 점수:", student['score'])

# 📘 예제 3
# 리스트 [1,2,2,3,3,3]을 집합으로 변환해 중복 제거하세요.
num_list = [1, 2, 2, 3, 3, 3]
num_set = set(num_list)
print("\n예제 3 - 중복 제거된 집합")
print("중복 제거된 집합:", num_set)
